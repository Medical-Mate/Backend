# CLAUDE.md

진료메이트 백엔드 저장소. 사람과 Claude Code가 함께 읽는 기준 문서입니다.

커밋·브랜치·PR 규칙은 [`.github/CONTRIBUTING.md`](.github/CONTRIBUTING.md)에 있습니다. 여기서는 반복하지 않고, **이 저장소에서만 통하는 맥락과 규칙**만 적습니다.

---

## 프로젝트

**진료메이트** — 환자가 진료 전 AI 문답으로 증상을 정리해 "브리핑 카드"를 만들고, 진료실에서 의사에게 전달하는 앱. 원티드 공모전 출품작.

- 팀은 네 트랙으로 나뉩니다: 디자인 2 · AI · 앱 · **백엔드(이 저장소)**
- 3주 MVP. 관리자용(병원 스태프) 화면은 이번 범위에서 제외
- **화면 번호는 두 가지가 섞여 있습니다.** 새 와이어프레임은 `1c`·`1f`·`1m-B` 같은 IA 코드를 쓰고, 초기 문서·코드 주석에는 `S1`~`S5` 표기가 남아 있습니다: S1 온보딩 / S1.5 부위 짚기 / S2 증상 문답 / S3 브리핑 카드 / S4 진료실 전달 / S5 진료 후 기록. **`S6 공유·재방문`은 없어졌습니다** — 공유 링크가 보류되면서 화면 자체가 빠졌습니다

**백엔드가 맡는 범위** — 인증, 저장, AI 서비스 호출, 카드 버전 관리, 진료실 전달, 기록·일정 조회. AI 모델·프롬프트·에이전트 구성은 별도 AI 레포 소관이고, **알림 발송은 앱 소관**입니다(아래 참고).

---

## 기술 스택

| | |
|---|---|
| 언어·런타임 | Java 21 (가상 스레드 사용) |
| 프레임워크 | Spring Boot 3.5.16 |
| 빌드 | Gradle 8.14 (Wrapper) |
| DB | PostgreSQL 16 |
| 영속성 | Spring Data JPA 단독 |
| 스키마 | Flyway 마이그레이션. `ddl-auto: validate` |
| 인증 | Spring Security · jjwt 0.12.7 |
| API 문서 | springdoc-openapi 2.9.0 (운영 포함 전 환경 공개) |
| 테스트 DB | Testcontainers PostgreSQL — **Docker 필요** |

---

## 자주 쓰는 명령

```bash
docker compose up -d          # 로컬 PostgreSQL
./gradlew bootRun             # 애플리케이션 실행
./gradlew build               # 컴파일 + 테스트
./gradlew test                # 테스트만
```

**테스트에는 Docker 가 떠 있어야 합니다.** 테스트 DB 를 Testcontainers 로 띄우기 때문입니다. H2 를 쓰지 않는 이유는 Flyway 마이그레이션이 PostgreSQL 문법이라, H2 로 검증하면 마이그레이션이 깨져도 테스트가 통과하기 때문입니다.

**스키마 변경은 마이그레이션 파일로만 합니다.** `src/main/resources/db/migration/` 에 `V2__...sql` 형태로 추가하세요. `ddl-auto` 는 `validate` 라 엔티티와 어긋나면 기동 시점에 실패합니다.

로컬 전용 설정은 `src/main/resources/application-local.yml.example`을 복사해서 씁니다. 복사본은 커밋되지 않습니다.

**5432가 다른 프로젝트에 물려 있으면** `DB_PORT`를 비어 있는 포트로 지정합니다. `docker-compose`와 Spring이 같은 변수를 읽으므로 한 곳만 바꾸면 됩니다. 셸이나 IDE 실행 구성의 환경변수로 넣으세요.

```bash
DB_PORT=5434 docker compose up -d
DB_PORT=5434 ./gradlew bootRun
```

앱을 컨테이너로 띄울 때는 DB 호스트가 `localhost`가 아니라 compose 서비스명이므로 `DB_HOST=postgres`를 함께 줍니다.

**Spring Boot는 `.env`를 스스로 읽지 않습니다.** `.env`는 컨테이너 실행(`docker-compose`의 `env_file`) 전용이고, IDE·`gradlew` 실행은 `application-local.yml`을 씁니다.

---

## 코드 구조

도메인형입니다. 각 도메인 패키지의 `package-info.java`에 무엇이 들어오는지와 결정 근거가 적혀 있으니, **새 코드를 넣기 전에 해당 패키지의 `package-info.java`를 먼저 읽으세요.**

```
com.jinryomate.backend
├── global/     공통. 도메인 지식을 두지 않는다
│   ├── error/  에러 규약
│   └── web/    요청 추적, 웹 공통
├── ai/         AI 서비스 클라이언트 (도메인 아님. intake·card가 공유)
├── auth/       카카오 로그인 · JWT · device(푸시 대상)
├── profile/      온보딩, 건강 프로필            S1
├── intake/       부위 짚기, 증상 문답 세션       S1.5 · S2 (1b~1i)
├── card/         브리핑 카드                   S3 (1e)
├── handoff/      진료실 전달(화면 보여주기)      S4 (1f)
├── visit/        진료 후 기록 · 기록 목록        S5 (1p · 1j)
├── appointment/  진료 예정 일정 · 캘린더         1r · 1n
└── home/         홈 요약 (여러 도메인을 모은다)   1n
```

`global`에 도메인 로직이 들어오면 잘못 놓인 것입니다. 이 패키지는 방치하면 잡동사니가 쌓이는 자리입니다.

---

## 작업 방식

### 순서

**이슈 → 구현 → 커밋 → PR → 머지.** 이슈를 건너뛰지 않습니다.

설계 판단이 들어간 작업은 이슈의 **비교한 대안**과 **재검토 트리거** 섹션을 반드시 채웁니다. 3주 뒤 발표에서 "왜 이렇게 했나"를 설명해야 하는데, 그 근거가 대화에만 있으면 남지 않습니다.

### 기술 선택

라이브러리·도구·아키텍처를 고를 때는 **하나를 골라 진행하지 말고 여러 방안을 트레이드오프와 함께 제시하고 결정을 받습니다.** 각 방안에 대해 ① 무엇을 얻고 ② 무엇을 포기하며 ③ 이 프로젝트의 제약(3주 일정, 백엔드 1인, 민감정보)에 비춰 왜 그런지를 씁니다. 추천은 하되 결정은 저장소 담당자가 합니다.

관례적 기본값이 명백한 사소한 선택까지 물어 시간을 뺏지는 않습니다. 그런 것은 근거를 밝히고 진행합니다.

### 결정을 남기는 곳

**노션 설계 결정 로그**가 단일 원천입니다. 아래 링크를 참고하세요.

설계 결정이 확정되는 순간(“이걸로 가자”, “확정” 같은 답이 나온 직후) **바로 기록합니다.** 나중에 몰아서 쓰면 근거가 이미 사라져 있습니다. Claude Code에는 `.claude/skills/design-decision` 스킬이 있어 이 상황에서 자동으로 동작합니다.

아직 안 정해진 것도 상태를 `재검토 필요`로 두고 기록합니다. 무엇을 기다리는지가 남아야 합니다.

---

## 도메인 규칙

구현할 때 반드시 지킬 것들입니다. 대부분 와이어프레임이나 개인정보 보호에서 나온 제약이라 임의로 바꾸면 안 됩니다.

### 민감정보

증상·복용약·기저질환·알레르기는 민감정보입니다.

- **예외 로깅에 요청 본문을 남기지 않습니다.** 증상 텍스트가 로그로 새면 안 됩니다
- **파일 로깅을 켜지 않습니다.** 평문으로 디스크에 쌓입니다. 운영에서는 배포 플랫폼의 로그 수집을 씁니다
- 계정 삭제 시 프로필·문답·카드·기록·일정까지 함께 지웁니다
- **파일 업로드를 받지 않습니다.** 사진 첨부가 와이어프레임에서 빠져서 `multipart`를 껐습니다. 되살릴 일이 생기면 `bytea`가 아니라 오브젝트 스토리지 + URL 저장으로 시작합니다

### "없어요"와 "잘 모르겠어요"는 다른 값입니다

`null` 하나로 뭉개면 의사용 카드에 `알레르기: 본인 확인 못 함`을 찍을 수 없습니다. 와이어프레임에 이미 있는 문구입니다.

**`status`가 두 벌입니다. 섞으면 안 됩니다.** 프로필 필드와 카드 축은 값 집합이 다릅니다.

**프로필 필드 — 3값** (우리가 정했습니다)

| `status` | 뜻 |
|---|---|
| `known` | 값이 있음 |
| `none` | 환자가 "없어요"라고 답함 |
| `unknown` | 환자가 "잘 모르겠어요"라고 답함 |

프로필 필드에는 출처(`self_input` / `kakao`)도 남깁니다. 나중에 카카오에서 값을 받아오게 되더라도 사용자가 직접 고친 값을 덮어쓰지 않기 위함입니다.

**카드 축 — 5값** (AI 계약이 정합니다. `schema/card.py`의 `FieldStatus`)

| `status` | 뜻 |
|---|---|
| `not_asked` | 아직 묻지 않음 — **기본값** |
| `filled` | 환자가 답했고 값이 있음 |
| `unknown` | 환자가 "모르겠다"고 함 |
| `skipped` | 환자가 답하지 않고 넘김 |
| `ambiguous` | 발화가 축을 건드렸지만 값을 확정 못 함 |

> **3값으로 검증하면 거의 모든 카드가 떨어집니다.** 진료 전 카드는 8축을 전부 `not_asked`로 만들어 두고 채워 나갑니다 — **1턴째 카드는 대부분의 축이 `not_asked`입니다.** `ambiguous`도 최종 카드에 남을 수 있습니다. AI는 1회만 되묻고, 그래도 확정이 안 되면 그 상태로 닫습니다.

**축마다 `source`도 옵니다.** 의사가 "환자가 말한 그대로"와 "나중에 고친 값"을 구별할 수 있어야 합니다.

| `source` | 언제 | `evidence` |
|---|---|---|
| `ai_extraction` | 발화에서 추출 (기본) | 환자 발화 원문 |
| `selection` | 칩·폼으로 고름 | `"[선택] 5점"` |
| `patient_edit` | 환자가 S3에서 고침 | `"[환자 수정] 3일 전부터"` |

**`patient_edit`은 우리가 채웁니다.** 편집은 S3 화면에서 일어나 AI를 거치지 않으므로, AI는 자리만 만들고 `ai_extraction`/`selection`까지만 채워 보냅니다. `status`는 `filled` 그대로 둡니다.

> `evidence`가 비지 않는 이유 — "근거 없는 값은 만들지 않는다"는 **AI가 근거 없이 값을 만들지 않는다**는 뜻입니다. 환자가 직접 쓴 값은 그 자체가 근거입니다.

### AI 응답은 저장 전에 검증합니다

AI 서비스가 준 카드는 그대로 저장하지 않습니다.

- 각 `text` 80자 이내 (낭독 모드 큰 글자 기준)
- `questions` 최대 3개, 각 40자
- `site.codes`는 **부위 마스터(`body-map`)에 없는 id면 거부** — 아래 참고
- `axes[].status`는 **5값 외 거부**, `axes[].source`는 3값 외 거부

**`title`은 검증하지 않습니다.** AI가 부위 + 기간을 **결정론으로 조합**해 만듭니다(`복부 통증 · 3주`). LLM을 거치지 않는 값의 조합이라 **병명이 들어갈 경로가 없습니다.**

**진료과를 우리가 판정하지 않습니다.** `suggested_department` enum 규칙은 버렸습니다. AI가 주는 `department_guidance`를 **그대로** 씁니다.

> **왜 버렸나** — 증상에서 과를 고르는 것은 **감별진단**입니다. AI가 하지 않기로 한 일을 백엔드가 규칙으로 대신하면 판정 주체만 바뀌고 **원칙은 우회됩니다.** `department_guidance`는 증상이 아니라 **환자가 짚은 부위 노드의 속성**이고, 배열이며(하나로 좁히는 순간 추천이 됩니다), `source`로 "의료인 자문 전"임을 함께 냅니다.
>
> 부위 34곳 중 **14곳은 진료과가 없어 `null`이 옵니다.** 그대로 내려보내고 **화면에서 진료과 줄을 숨기는 것은 앱 몫**입니다. 상위 앵커 값으로 대체하지 않습니다 — "무릎을 짚었는데 다리 과를 보여준다"가 됩니다.
>
> 한 부위에 여러 과가 붙습니다. 아랫배(`SUR:032`)는 `내과 · 소화기내과 · 산부인과 · 비뇨의학과` 넷입니다. **하나로 고르는 순간 그게 감별입니다.**

**대신 닫힌 목록으로는 검증합니다.** 온톨로지에 등장하는 진료과는 13개가 전부이고, 늘어나면 AI 담당이 먼저 알려줍니다. enum을 버린 것이 검증을 포기한 것은 아닙니다 — **하나로 좁히지 않을 뿐, 값 자체가 목록 밖이면 거부합니다.**

```
가정의학과 내과 비뇨의학과 산부인과 소화기내과 신경과 신경외과
심장내과 안과 이비인후과 정형외과 피부과 호흡기내과
```

검증에 걸린 필드는 `unknown`으로 저장해 환자가 S3 화면에서 직접 채우게 합니다. 카드 생성이 통째로 실패해서 문답이 날아가는 일은 없어야 합니다.

**재요청은 하지 않습니다.** 턴 API에 카드만 다시 만드는 경로가 없어서, 같은 `state`로 다시 부르면 질문이 하나 더 나갑니다. 매 턴 카드가 갱신되므로 재요청의 값어치도 원래보다 작습니다.

응답의 `meta.pipelineVersion`을 카드와 함께 저장합니다. 프롬프트는 AI 레포에서 관리하므로, 백엔드가 추적성을 확보하는 유일한 수단입니다.

### 확정된 카드는 수정하지 않습니다

`draft` → 환자 수정 → `confirmed`. 확정 이후의 변경은 **새 버전으로만** 남깁니다. 의사가 본 카드가 뒤바뀌면 안 됩니다.

**저장 시점은 `ended: true` 턴입니다.** 그 턴의 카드가 최종입니다. 환자가 중간에 나가면(`stop`) 그때까지의 마지막 카드가 결과이고, 문답을 끝까지 강제하지 않습니다.

**종료 뒤 턴을 또 보내도 새 버전을 만들지 않습니다.** AI 엔진이 마무리 문장만 돌려주고 카드를 건드리지 않아서, 응답 카드가 직전과 같습니다.

### AI 서비스는 무상태입니다

자체 DB를 두지 않고, 호출마다 필요한 컨텍스트를 전부 받습니다. 저장은 백엔드가 전담합니다 — 환자 데이터가 두 군데로 쪼개지면 개인정보 처리 범위가 두 배가 됩니다.

앱은 AI 서비스를 직접 부르지 않습니다. **앱 → 백엔드 → AI 서비스** 한 방향입니다.

**계약은 AI 저장소가 단일 원천입니다.** 여기에 베끼지 않습니다 — 베끼면 한쪽이 낡습니다.

[`api-previsit.md`](https://github.com/Medical-Mate/AI/blob/main/docs/api-previsit.md) · [`api-postvisit.md`](https://github.com/Medical-Mate/AI/blob/main/docs/api-postvisit.md) · [`schema/export.py`](https://github.com/Medical-Mate/AI/blob/main/src/medimate/schema/export.py)

| | |
|---|---|
| 경로 | `POST /v1/previsit/sessions` · `/v1/previsit/turns` · `/v1/postvisit/memo` |
| 호출 방식 | 턴마다 `{state, utterance}` 를 보내고 돌아온 `state` 로 덮어씁니다 |
| 카드 | **매 턴 응답에 들어 있습니다.** 카드만 따로 만드는 경로는 없습니다 |
| 질문 후보 | `question_candidates: true` 를 요청에 실어야 옵니다. **기본은 꺼짐**이고 종료 턴에만 옵니다 |
| 오류 | `422` 버그(재시도 무의미) · `502` 같은 `state` 로 1회 재시도 · `503` 운영 알림 |

**질문 후보를 요청 필드로 켜는 이유는 비용입니다.** 후보 생성은 Bedrock 호출이고 그 토큰이 서버와 **같은 크레딧에서 나갑니다**. 자동으로 돌면 끌 방법이 없는데, 필드로 켜면 크레딧이 마르거나 Bedrock이 죽었을 때 우회로가 생깁니다.

**`state`는 절대 열어보지 않습니다.** `TEXT`로 통째 보관하고 턴마다 덮어쓸 뿐입니다. 계약이 "불투명하게 다루라"고 명시했고, 내부 구조에 의존하는 순간 AI 쪽 변경이 우리를 깨뜨립니다.

**서명은 HMAC-SHA256입니다.** `X-Signature` · `X-Timestamp` · `X-Request-Id` 헤더를 쓰고, 서명 대상은 이렇습니다.

```
message = f"{method}.{path}.{timestamp}.{request_id}." + body_bytes
```

`path`는 **쿼리스트링 제외, 앞 `/` 포함**입니다. 메서드는 대문자이고 `request_id`가 없으면 그 자리는 빈 문자열입니다. 본문은 **직렬화된 그대로의 바이트**여야 합니다 — 재직렬화하면 공백·키 순서가 달라져 서명이 틀어집니다. 한글을 `\uXXXX`로 이스케이프하면 안 됩니다(UTF-8 원바이트). 타임스탬프 허용 오차는 ±300초이고, 논스 중복 차단은 **우리 몫**입니다.

**`GET`도 서명이 필요합니다.** 면제는 `/health` `/docs` `/openapi.json` `/redoc` 뿐입니다. GET은 본문이 없으니 `body_bytes`를 빈 바이트로 계산합니다.

**구현은 테스트 벡터로 검증합니다.** AI 저장소의 [`docs/examples/hmac-vectors.json`](https://github.com/Medical-Mate/AI/blob/main/docs/examples/hmac-vectors.json)에 10개가 있고, 벡터마다 한 가지씩만 달라서 틀렸을 때 어디서 갈렸는지 좁힙니다. 규격이 바뀌면 저쪽 테스트에서 벡터 파일이 먼저 깨집니다.

**`request_id`는 본문 최상위입니다.** 모든 요청에 실어 보내고 응답 최상위 `request_id`로 그대로 돌아옵니다. 장애 시 양쪽 로그를 잇는 유일한 열쇠입니다.

> 우리 규칙은 원래 `meta.requestId`였는데 **AI 쪽 이름에 맞췄습니다.** 규칙의 취지는 "회수해서 양쪽 로그를 잇는다"이고 그건 어느 이름이든 지켜집니다. 이름 때문에 남의 API를 고치게 할 일이 아니고, 앱(안드로이드)도 이미 그 형태로 붙어 있습니다.

헤더 `X-Request-Id`는 **서명 계산용으로 별개**입니다. 본문과 같은 값을 보내야 로그가 한 줄로 이어집니다.

### 부위 마스터는 AI가 냅니다

`GET /v1/ontology/body-map` 이 단일 원천입니다. 앵커 9 + 구역 25 = **34곳**이고 응답 예시 전체가 [`docs/examples/body-map.json`](https://github.com/Medical-Mate/AI/blob/main/docs/examples/body-map.json)에 있습니다.

- id는 `ANC:001`(앵커) · `SUR:032`(구역) 형식입니다. **`siteCodes`는 이 id로 받습니다**
- `image_key`는 앱 이미지 자산 키입니다. **이미지는 앱 자산이고 서버는 키만 냅니다**
- `aliases`는 폼 검색용 유의어인데 **검색은 앱이 로컬에서 합니다**(한 글자마다 호출하므로). 서버 왕복이 없습니다
- 갱신 여부는 `ontology_snapshot` 문자열로 판단합니다

**기동할 때 받아오지 않습니다.** 그러면 AI가 떠 있어야 백엔드가 뜹니다 — AI 없이도 기동돼야 합니다.

### 진료실 전달 — 환자가 화면을 보여줍니다

**QR도 공유 링크도 쓰지 않습니다.** 화면 1f(폰 직접 전달)로 확정됐습니다.

QR은 관리자용 화면이 빠지면서 찍을 주체가 사라졌고, 서버가 이미지를 그려주면 병원에서 신호가 약할 때 그 자리에서 표시조차 안 됩니다.

공유 링크는 새 와이어프레임에서 보류(IA의 `1k · 링크로 전달`)가 되어 **통째로 지웠습니다.** 인증 없이 열리는 경로(`/s/{token}`)를 안 쓰면서 열어두면 공격 면만 남습니다. 되살릴 일이 생기면 그 커밋을 되돌리는 데서 시작합니다.

- 전달 화면(`/api/cards/{id}/handoff`)에는 **의사가 볼 것만** 담습니다. `evidence`·`pipelineVersion` 같은 내부 추적값을 의사에게 보여줄 이유가 없습니다
- **확정한 카드만** 전달합니다. 초안을 의사가 보면 안 됩니다
- 여는 순간이 `handedOffAt`에 남습니다. 나중에 "진료 어떠셨어요"를 물을 근거입니다

### 카카오 로그인

- 동의항목은 **회원번호 · 이름 · 성별 · 출생연도 · 생일**입니다 (비즈니스 앱 전환). 이메일·전화번호는 받지 않습니다
- **생일과 출생연도는 별개 동의항목입니다.** 생일만 켜면 `MMDD`만 와서 나이를 만들 수 없습니다
- **값이 없는 경우가 정상 경로입니다.** 선택 동의는 거부할 수 있고 검수 전에는 항목이 안 내려옵니다. 카카오 조회가 실패해도 로그인을 막지 않고, 온보딩 입력만으로 동작해야 합니다
- 나이가 아니라 **출생연도 + 생일**을 저장합니다. 나이는 해가 바뀌면 낡고, 카카오 연령대(`30~39`)로는 카드의 `32세`를 못 만듭니다. **음력 생일은 나이 계산에서 제외**합니다 — 양력 변환 없이 쓰면 오히려 틀립니다
- **앱이 보낸 카카오 토큰을 그대로 믿지 않습니다.** 서버가 카카오에 재검증하고, 토큰의 앱 ID가 우리 앱인지까지 확인한 뒤 자체 JWT를 발급합니다
- **`KAKAO_APP_ID`가 없으면 서버가 뜨지 않습니다.** 앱 ID 대조는 남의 앱 토큰을 걸러내는 유일한 방어선인데, 값이 없을 때 그냥 통과시키면 방어선이 꺼진 채로 로그인이 멀쩡히 성공해서 아무도 눈치채지 못합니다. 배포에서 환경변수 하나 빠뜨리는 것만으로 뚫리므로, 조용히 넘어가는 대신 기동을 막습니다
- **refresh 응답의 `onboardingRequired`도 매번 계산합니다.** 앱은 재실행 시 이 값으로 온보딩/홈을 정하므로, 로그인과 다른 값을 주면 빈 프로필로 홈에 들어갑니다
- refresh 토큰은 **해시로 저장**하고 쓰면 즉시 폐기합니다(회전)
- 카카오 토큰은 저장하지 않습니다
- `device`는 로그인 수단이 아니라 **푸시 대상**입니다 (`user` 1 : `device` N)
- **`POST /api/me/devices`는 현재 쓰이지 않습니다.** 알림을 앱 로컬 예약으로 정해서 서버가 푸시를 보내지 않습니다. 서버 푸시로 옮길 때 쓸 자리로 남겨둔 것입니다

### 카카오 연결 해제는 웹훅으로 받습니다

사용자가 **카카오 설정에서** 우리 앱 연결을 끊으면 카카오가 `POST /webhooks/kakao/unlink` 를 부릅니다. 그 회원의 데이터를 지웁니다.

이게 없으면 그 사람의 증상·복용약이 DB에 **영영 남습니다.** 민감정보를 다루는 서비스라 "더 이상 쓰지 않겠다"는 의사 표시를 받아야 합니다.

- **인증 없이 열리는 경로**입니다. 어드민 키(`Authorization: KakaoAK ...`) 대조가 유일한 방어선이고, `app_id` 도 함께 봅니다 — 한 어드민 키가 여러 앱을 관리할 수 있습니다
- **성공·실패와 무관하게 200** 을 줍니다. 오류를 내면 카카오가 웹훅을 비활성화할 수 있습니다
- 없는 회원번호여도 조용히 넘어갑니다. 웹훅이 재전송되거나 우리가 먼저 탈퇴 처리한 뒤 도착할 수 있습니다
- **카카오 콘솔의 앱 설정 → 웹훅에 주소를 등록해야** 동작합니다

> 카카오는 3초 이내 200을 기대하고 연결 해제 웹훅은 **재전송하지 않습니다.** Render 무료 플랜에서는 15분 유휴 후 잠들어 이 이벤트가 유실됐습니다. **Lightsail로 옮기면서 상시 기동이 되어 해결됐습니다.**

### 알림은 앱이 예약합니다

**서버가 푸시를 보내지 않습니다.** 이 결정은 Render 무료 플랜이 15분 유휴 후 잠들어 `@Scheduled`가 아예 동작하지 않는다는 제약에서 나왔습니다.

**Lightsail로 옮기면서 그 제약은 사라졌지만, 결정은 유지합니다.** 서버 푸시로 가려면 Firebase 프로젝트·서비스 계정 키·앱 SDK가 선행되고, 3주 일정에서 그 비용이 큽니다. 앱 로컬 예약으로도 데모 요구는 충족됩니다.

일정 조회(`GET /api/me/appointments`)가 `scheduledAt`·`clinicName`·`department`·`purpose`·`cardTitle`을 다 내려주므로 앱이 기기에 예약할 재료가 전부 있습니다.

- **알림 문구를 서버가 만들지 않습니다.** 홈의 "지난 진료 후 n일"과 같은 이유입니다 — 문구를 바꿀 때마다 배포해야 하고, 시간대가 어긋나면 날짜가 틀립니다
- 여러 기기 동기화는 포기했습니다. 기기를 바꾸면 예약이 따라가지 않습니다
- 서버 푸시가 필요해지면 **외부 크론이 엔드포인트를 깨우는 방식**으로 올립니다. 유료 플랜으로 가면 `@Scheduled`가 바로 가능해집니다

### 녹음은 저장하지 않습니다

S5 되묻기는 기억 재구성 방식입니다. 오디오 컬럼 자체를 만들지 않습니다.

---

## 배포

**AWS Lightsail 한 대**(서울, 2 vCPU · 2GB)에 Caddy · 백엔드 · AI · Postgres를 전부 올립니다. 자세한 절차는 [`docs/deploy-lightsail.md`](docs/deploy-lightsail.md).

| | |
|---|---|
| 정식 주소 | `https://api.medicalmate.site` |
| CDN 경유 | `https://d3f36x6ccm838d.cloudfront.net` |
| 고정 IP | `54.116.115.128` |
| Swagger | 위 주소 + `/swagger-ui.html` |

**앱·AI 담당자에게는 CloudFront 주소를 줍니다.** 일부 기관 방화벽(FortiGate)이 신규 등록 도메인을 `Newly Observed Domain`으로 24~72시간 차단하는데, `cloudfront.net`은 그 분류에 걸리지 않습니다. 발표장 네트워크에서도 같은 이유로 더 안전합니다.

**배포는 `main`에 머지하면 자동입니다.** GitHub Actions가 GHCR에 이미지를 올리고, 서버에서 `docker compose pull && up -d`로 갈아끼웁니다. **서버에서 빌드하지 않습니다** — 2GB에 컨테이너가 넷 올라가는데 거기서 Gradle을 돌리면 OOM입니다.

**인터넷에 열리는 것은 Caddy뿐입니다.** AI와 Postgres는 호스트 포트를 열지 않아 밖에서 보이지 않습니다. 방화벽은 80·443만 열고 22는 관리자 IP로 제한합니다.

> **비용**: 월 $12 (신규 3개월 무료). AWS 크레딧 $120/182일 안에서 씁니다. **Bedrock 토큰 비용이 같은 크레딧에서 나가므로** 서버를 더 키우기 전에 잔액을 먼저 봅니다. 예산 알림이 $30에 걸려 있습니다.

---

## 설계 결정 로그

**노션에서 관리합니다.** 저장소에 표를 두면 두 곳이 갈라져서 한쪽이 낡습니다.

[설계 결정 로그 (Notion)](https://app.notion.com/p/1cea038a97e74046b5158c4bab472538) · [진료메이트 프로젝트 페이지](https://app.notion.com/p/3d0eaf19306981efacd6cd60ffbfd25f)

각 행은 `결정 · 날짜 · 영역 · 상태 · 검토한 대안 · 선택 근거 · 재검토 트리거 · 관련 링크`를 가집니다. 상태가 `재검토 필요`인 항목은 아래 미확정 항목과 같은 것들입니다.

### 언제 기록하나

**되돌리는 데 비용이 드는 선택을 했을 때** 기록합니다. 라이브러리·프레임워크 채택, 스키마 구조, 인증 방식, 외부 연동 형태, 저장하지 않기로 한 데이터 같은 것들입니다.

변수명이나 메서드 분리처럼 코드를 읽으면 알 수 있는 것, 되돌리는 데 5분이면 되는 것은 기록하지 않습니다. 로그가 길어지면 아무도 안 읽습니다.

Claude Code로 작업 중이라면 `/design-decision` 스킬이 노션에 바로 씁니다. 자세한 내용은 `.claude/skills/design-decision/SKILL.md`.

---

## 미확정 항목

구현 중 마주치면 임의로 정하지 말고 확인을 받으세요.

| 항목 | 정해지면 바뀌는 것 | 기다리는 답 |
|---|---|---|
| **`siteCodes`에 좌우를 어떻게 싣나** | `laterality: "left_right"`인 노드가 **21개인데 id는 하나뿐**입니다. `SUR:051` 하나가 왼쪽·오른쪽을 다 덮으므로 좌우는 별도 필드여야 합니다. `selections`의 `{axis:"site", value:?}`에 무엇을 넣는지 | AI 담당 |
| **`ontology_snapshot` 갱신을 어떻게 아나** | 저장소에 JSON을 박고 바뀌면 알림받는 쪽을 제안했습니다. 폴링을 붙일지 여부가 갈립니다 | AI 담당 |
| **앱이 보내는 `siteCodes` 형식** | 지금 `hand_finger_joint_R` 같은 **지어낸 형식**을 검증 없이 받습니다. 앱이 `SUR:032`를 그대로 싣는지 확인되면 id 집합으로 검증합니다 | 앱 |
| **온보딩 이름 입력칸** | 카드에 찍히는 이름의 출처 | 디자인 |
| **퍼짐·동반 증상 축** | 카드에 자리가 없습니다. 진료실 전달 화면에서 쓰면 유지, 안 쓰면 SOCRATES 8축에서 2축이 빠져 문답이 그만큼 짧아집니다 | 디자인 |
| **`department_guidance` 표기** | `source`("의료인 자문 확인 전")를 화면에 노출해야 하고 "추천" 표현은 금지입니다. 진료과가 없는 부위 14곳은 줄을 숨겨야 합니다 | 디자인 |
| **STT를 누가 하는가** | 앱이면 지금 구조 그대로, 서버면 오디오 업로드가 생겨 "녹음 저장 안 함" 원칙과 부딪힙니다 | 앱 |

**해결된 것** — `AI 서비스 배포 위치`(같은 호스트 Docker 네트워크), `HMAC 서명 규격`(벡터 10개로 대조 완료), `문답 단계 수`(화면 4단계 ≠ 대화 턴), `MEDIMATE_PROVIDER`/`MEDIMATE_MODEL`(Bedrock Nova Pro), `red_flags`·`patient_notes`(각각 자문 전 자리·축 밖 환자 말), `병원 검색 화면`(와이어프레임 `1m-B`에 있음), `/ai/card 응답 시간`(카드가 매 턴 오므로 별도 호출 없음), `환자 수정본의 evidence`(축마다 `source` 3값), `카드 저장 시점`(`ended: true` 턴이 최종, 이후 턴은 카드를 건드리지 않음), `request_id 회수`(본문 최상위로 그대로 돌아옴), `부위 마스터 목록`(`GET /v1/ontology/body-map`).
