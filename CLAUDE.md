# CLAUDE.md

진료메이트 백엔드 저장소. 사람과 Claude Code가 함께 읽는 기준 문서입니다.

커밋·브랜치·PR 규칙은 [`.github/CONTRIBUTING.md`](.github/CONTRIBUTING.md)에 있습니다. 여기서는 반복하지 않고, **이 저장소에서만 통하는 맥락과 규칙**만 적습니다.

---

## 프로젝트

**진료메이트** — 환자가 진료 전 AI 문답으로 증상을 정리해 "브리핑 카드"를 만들고, 진료실에서 의사에게 전달하는 앱. 원티드 공모전 출품작.

- 팀은 네 트랙으로 나뉩니다: 디자인 2 · AI · 앱 · **백엔드(이 저장소)**
- 3주 MVP, 9화면. 관리자용(병원 스태프) 화면은 이번 범위에서 제외
- 화면 번호는 와이어프레임 기준입니다: S1 온보딩 / S1.5 부위 짚기 / S2 증상 문답 / S3 브리핑 카드 / S4 진료실 전달 / S5 진료 후 기록 / S6 공유·재방문

**백엔드가 맡는 범위** — 인증, 저장, AI 서비스 호출, 카드 버전 관리, QR·공유 링크, 알림. AI 모델·프롬프트·에이전트 구성은 별도 AI 레포 소관입니다.

---

## 기술 스택

| | |
|---|---|
| 언어·런타임 | Java 21 (가상 스레드 사용) |
| 프레임워크 | Spring Boot 3.5.16 |
| 빌드 | Gradle 8.14 (Wrapper) |
| DB | PostgreSQL 16 |
| 영속성 | Spring Data JPA 단독 |
| 스키마 | `ddl-auto: update` — 안정되면 Flyway로 전환 |

---

## 자주 쓰는 명령

```bash
docker compose up -d          # 로컬 PostgreSQL
./gradlew bootRun             # 애플리케이션 실행
./gradlew build               # 컴파일 + 테스트
./gradlew test                # 테스트만
```

로컬 전용 설정은 `src/main/resources/application-local.yml.example`을 복사해서 씁니다. 복사본은 커밋되지 않습니다.

**5432가 다른 프로젝트에 물려 있으면** `DB_PORT`를 비어 있는 포트로 지정합니다. `docker-compose`와 Spring이 같은 변수를 읽으므로 한 곳만 바꾸면 됩니다. 셸이나 IDE 실행 구성의 환경변수로 넣으세요.

```bash
DB_PORT=5434 docker compose up -d
DB_PORT=5434 ./gradlew bootRun
```

앱을 컨테이너로 띄울 때는 DB 호스트가 `localhost`가 아니라 compose 서비스명이므로 `DB_HOST=postgres`를 함께 줍니다.

**Spring Boot는 `.env`를 스스로 읽지 않습니다.** `.env`는 컨테이너 실행(`docker-compose`의 `env_file`) 전용이고, IDE·`gradlew` 실행은 `application-local.yml`을 씁니다.

---

## 코드 구조

도메인형입니다. 각 도메인 패키지의 `package-info.java`에 무엇이 들어오는지와 결정 근거가 적혀 있으니, **새 코드를 넣기 전에 해당 패키지의 `package-info.java`를 먼저 읽으세요.**

```
com.jinryomate.backend
├── global/     공통. 도메인 지식을 두지 않는다
│   ├── error/  에러 규약
│   └── web/    요청 추적, 웹 공통
├── ai/         AI 서비스 클라이언트 (도메인 아님. intake·card가 공유)
├── auth/       카카오 로그인 · JWT · device(푸시 대상)
├── profile/    온보딩, 건강 프로필          S1
├── intake/     부위 짚기, 증상 문답 세션      S1.5 · S2
├── card/       브리핑 카드                 S3
├── handoff/    QR · 공유 링크              S4 · S6
└── visit/      진료 후 기록, 되묻기          S5
```

`global`에 도메인 로직이 들어오면 잘못 놓인 것입니다. 이 패키지는 방치하면 잡동사니가 쌓이는 자리입니다.

---

## 작업 방식

### 순서

**이슈 → 구현 → 커밋 → PR → 머지.** 이슈를 건너뛰지 않습니다.

설계 판단이 들어간 작업은 이슈의 **비교한 대안**과 **재검토 트리거** 섹션을 반드시 채웁니다. 3주 뒤 발표에서 "왜 이렇게 했나"를 설명해야 하는데, 그 근거가 대화에만 있으면 남지 않습니다.

### 기술 선택

라이브러리·도구·아키텍처를 고를 때는 **하나를 골라 진행하지 말고 여러 방안을 트레이드오프와 함께 제시하고 결정을 받습니다.** 각 방안에 대해 ① 무엇을 얻고 ② 무엇을 포기하며 ③ 이 프로젝트의 제약(3주 일정, 백엔드 1인, 민감정보)에 비춰 왜 그런지를 씁니다. 추천은 하되 결정은 저장소 담당자가 합니다.

관례적 기본값이 명백한 사소한 선택까지 물어 시간을 뺏지는 않습니다. 그런 것은 근거를 밝히고 진행합니다.

### 결정을 남기는 곳

아래 **설계 결정 로그**에 한 줄로 남기고, 자세한 근거는 이슈에 둡니다.

---

## 도메인 규칙

구현할 때 반드시 지킬 것들입니다. 대부분 와이어프레임이나 개인정보 보호에서 나온 제약이라 임의로 바꾸면 안 됩니다.

### 민감정보

증상·복용약·기저질환·알레르기는 민감정보입니다.

- **예외 로깅에 요청 본문을 남기지 않습니다.** 증상 텍스트가 로그로 새면 안 됩니다
- **파일 로깅을 켜지 않습니다.** 평문으로 디스크에 쌓입니다. 운영에서는 배포 플랫폼의 로그 수집을 씁니다
- 계정 삭제 시 카드·기록·첨부까지 함께 지웁니다

### "없어요"와 "잘 모르겠어요"는 다른 값입니다

프로필과 카드의 모든 필드는 3값을 구분합니다.

| `status` | 뜻 |
|---|---|
| `known` | 값이 있음 |
| `none` | 환자가 "없어요"라고 답함 |
| `unknown` | 환자가 "잘 모르겠어요"라고 답함 |

`null` 하나로 뭉개면 의사용 카드에 `알레르기: 본인 확인 못 함`을 찍을 수 없습니다. 와이어프레임에 이미 있는 문구입니다.

프로필 필드에는 출처(`self_input` / `kakao`)도 남깁니다. 나중에 카카오에서 값을 받아오게 되더라도 사용자가 직접 고친 값을 덮어쓰지 않기 위함입니다.

### AI 응답은 저장 전에 검증합니다

AI 서비스가 준 카드는 그대로 저장하지 않습니다.

- `title` 20자 이내, **진단명·질환명 금지**
- 각 `text` 80자 이내 (낭독 모드 큰 글자 기준)
- `questions` 최대 3개, 각 40자
- `suggested_department`는 정해진 enum 외 거부
- `site.codes`는 부위 마스터에 없으면 거부

검증에 걸리면 **1회 재요청**하고, 그래도 실패한 필드는 `unknown`으로 저장해 환자가 S3 화면에서 직접 채우게 합니다. 카드 생성이 통째로 실패해서 문답이 날아가는 일은 없어야 합니다.

응답의 `meta.pipelineVersion`을 카드와 함께 저장합니다. 프롬프트는 AI 레포에서 관리하므로, 백엔드가 추적성을 확보하는 유일한 수단입니다.

### 확정된 카드는 수정하지 않습니다

`draft` → 환자 수정 → `confirmed`. 확정 이후의 변경은 **새 버전으로만** 남깁니다. 의사가 본 카드가 뒤바뀌면 안 됩니다.

### AI 서비스는 무상태입니다

자체 DB를 두지 않고, 호출마다 필요한 컨텍스트를 전부 받습니다. 저장은 백엔드가 전담합니다 — 환자 데이터가 두 군데로 쪼개지면 개인정보 처리 범위가 두 배가 됩니다.

앱은 AI 서비스를 직접 부르지 않습니다. **앱 → 백엔드 → AI 서비스** 한 방향입니다.

모든 요청에 `requestId`를 실어 보내고 응답 `meta.requestId`로 돌려받습니다. 장애 시 양쪽 로그를 잇는 유일한 열쇠입니다.

### 공유 토큰과 QR

관리자용 화면이 없어 QR은 인증 없이 열립니다.

- **QR 페이로드에는 토큰만** 담습니다. 이름이나 증상이 들어가면 접수 데스크에서 화면이 찍히는 것만으로 유출됩니다
- 토큰은 추측 불가능해야 하고, **만료가 유일한 방어선**입니다. 만료 없는 공유 링크를 만들지 않습니다

### 카카오 로그인

- 동의항목은 **회원번호만**. 이름·나이·성별은 온보딩에서 직접 입력받습니다
- **앱이 보낸 카카오 토큰을 그대로 믿지 않습니다.** 서버가 카카오에 재검증하고, 토큰의 앱 ID가 우리 앱인지까지 확인한 뒤 자체 JWT를 발급합니다
- 카카오 토큰은 저장하지 않습니다
- `device`는 로그인 수단이 아니라 **푸시 대상**입니다 (`user` 1 : `device` N)

### 녹음은 저장하지 않습니다

S5 되묻기는 기억 재구성 방식입니다. 오디오 컬럼 자체를 만들지 않습니다.

---

## 설계 결정 로그

| 결정 | 대안 → 선택 근거 | 이슈 |
|---|---|---|
| Java 21 + Gradle | Java 17 → **21**. AI 호출이 요청당 수 초 I/O 대기라 가상 스레드가 실이득 | [#2](https://github.com/Medical-Mate/Backend/issues/2) |
| Spring Boot 3.5.16 | 4.1.1 → **3.5.16**. Security 설정이 버전마다 바뀌어 카카오 OAuth 예제 불일치 리스크 | [#2](https://github.com/Medical-Mate/Backend/issues/2) |
| PostgreSQL | MySQL → **PG**. 카드 필드 확정 전이라 `jsonb`로 마이그레이션 횟수를 줄임 | [#2](https://github.com/Medical-Mate/Backend/issues/2) |
| Spring Data JPA 단독 | QueryDSL·MyBatis → **JPA**. MVP에 검색·필터·정렬이 없음 | [#2](https://github.com/Medical-Mate/Backend/issues/2) |
| `ddl-auto`로 시작 | Flyway 처음부터 → **`ddl-auto`**. 1주차 스키마 변동 구간의 속도를 택함 | [#2](https://github.com/Medical-Mate/Backend/issues/2) |
| 도메인형 패키지 | 계층형 → **도메인형**. 도메인이 화면 단위로 이미 갈림 | [#2](https://github.com/Medical-Mate/Backend/issues/2) |
| AI는 별도 서비스 | 백엔드 직접 프록시 → **별도 서비스**. 멀티 에이전트는 파이썬 생태계가 낫고 역할 경계가 선명해짐 | [#2](https://github.com/Medical-Mate/Backend/issues/2) |
| 카카오 로그인, 회원번호만 | 기기 토큰·SMS → **카카오**. 이름·성별 등은 비즈앱 검수가 붙어 3주 크리티컬 패스에 외부 심사가 올라감 | [#2](https://github.com/Medical-Mate/Backend/issues/2) |
| 콘솔 로깅만 | 파일 로깅 → **콘솔**. 증상이 평문으로 디스크에 쌓이는 것을 피함 | [#2](https://github.com/Medical-Mate/Backend/issues/2) |

---

## 미확정 항목

구현 중 마주치면 임의로 정하지 말고 확인을 받으세요.

| 항목 | 정해지면 바뀌는 것 | 기다리는 답 |
|---|---|---|
| **AI 서비스 배포 위치** | 서버 간 인증 방식. 같은 사설망이면 공유 시크릿 헤더, 공개 인터넷이면 HMAC 서명 | AI 담당 |
| **`/ai/card` 응답 시간 자릿수** | 10초대면 동기, 분 단위면 비동기(작업 접수 + 폴링/푸시). 앱 작업량도 함께 바뀜 | AI 담당 |
| **`request_id` 멱등 처리 가능 여부** | 불가하면 재시도를 아예 제거. 중복 과금이 더 큰 손해 | AI 담당 |
| **QR을 누가 무엇으로 찍는가** | 공유 뷰의 화면 폭과 토큰 만료 시간 | 앱·디자인 |
| **온보딩 이름 입력칸** | 카드에 찍히는 이름의 출처 | 디자인 |

그때까지의 임시 방침: 서버 간 인증은 공유 시크릿, AI 호출은 동기, 재시도는 1회, 만료는 설정값.
