/**
 * AI 서비스 연동.
 * 
 * <p>도메인이 아니라 외부 시스템 클라이언트다. intake와 card가 함께 쓰기 때문에
 * 둘 중 어느 쪽에도 두지 않았다.
 * 
 * <p>AI 서비스는 무상태다. 호출할 때마다 필요한 컨텍스트를 전부 실어 보내고,
 * 저장은 백엔드가 전담한다. 환자 데이터가 두 군데로 쪼개지지 않게 하기 위함이다.
 * 
 * <p>엔드포인트는 두 개다.
 * <ul>
 *   <li>{@code POST /ai/turn} — 다음 질문 생성 (S2, 세션당 6회 안팎)</li>
 *   <li>{@code POST /ai/card} — 대화를 브리핑 카드로 (S3, 세션당 1회)</li>
 * </ul>
 */
package com.jinryomate.backend.ai;
