/**
 * 진료 예정 일정. 화면 1r(캘린더) · 1n(홈의 다가오는 일정).
 *
 * <p>진료 기록({@code visit})과 <b>다른 테이블이다.</b> 기록은 다녀온 뒤 남기고 일정은
 * 가기 전에 만든다. 생명주기가 반대라 한 테이블에 두면 "아직 안 간 기록"이라는 이상한
 * 상태가 생긴다. 일정 없이 기록만 남기는 경우도, 일정만 잡고 안 가는 경우도 있다.
 *
 * <p>카드 연결은 선택이다. 캘린더의 {@code +} 버튼은 카드 없이도 "다음 주 치과"를
 * 적을 수 있어야 한다.
 *
 * <p><b>D-day 는 서버가 계산하지 않는다.</b> 날짜만 주고 앱이 센다. 서버 시간대와
 * 사용자 시간대가 어긋나면 "D-1"이 하루 틀린다.
 */
package com.jinryomate.backend.appointment;
