/**
 * 웹 데모 전용 경로. <b>심사가 끝나면 통째로 지운다.</b>
 *
 * <p>스토어 제출이 불가해 9/20 까지 웹 데모를 내고 심사 기간(~10/17) 공개를 유지한다.
 * 브라우저는 HMAC 시크릿을 들 수 없어 AI 를 직접 못 부르므로, 백엔드가 서명해서
 * 넘겨주는 통로가 필요하다.
 *
 * <p><b>저장하지 않는다.</b> 무인증 경로로 들어온 증상·복용약을 DB 에 쌓으면 지울 주체
 * (계정)가 없어 탈퇴로도 못 지운다. 여기는 순수 통로이고 {@code state} 는 브라우저가
 * 들고 다닌다 — AI 가 무상태라 가능한 구조다.
 *
 * <p><b>AI 가 하는 일만 연다.</b> 문진과 메모 정리 둘이다. 카드·진료 기록·일정은 우리
 * 도메인이라 프록시로 되지 않고, 그것까지 무인증으로 열면 데모가 아니라 계정 없는
 * 서비스가 된다.
 *
 * <p>메모 정리를 연 것은 <b>받은 텍스트를 분류해 돌려줄 뿐 아무것도 남기지 않아서</b>다.
 * 카드가 필요 없고 이력도 없다 — 문진 통로와 성질이 같다. 처음에는 닫아 뒀는데, 그때는
 * 기록을 쌓아 보여주는 화면을 가정했었다.
 *
 * <p><b>이 패키지는 도메인이 아니다.</b> 안에 도메인 지식을 두지 않는다 — 본문을 열어보지
 * 않고 넘기는 것이 유일한 규칙이고, 그래서 AI 계약이 바뀌어도 여기는 안 바뀐다.
 *
 * <p>지울 때는 이 패키지와 {@code SecurityConfig} 의 {@code /api/demo/**} 한 줄을 지우면
 * 된다. 다른 곳에 손댄 것이 없다.
 */
package com.jinryomate.backend.demo;
