/**
 * 진료실 전달. 화면 1f.
 *
 * <p><b>환자가 자기 화면을 의사에게 보여준다.</b> QR 도 공유 링크도 쓰지 않는다.
 *
 * <p>QR 은 관리자용 화면이 빠지면서 찍을 주체가 사라졌고, 서버가 이미지를 그려주면
 * 병원에서 신호가 약할 때 그 자리에서 표시조차 안 된다.
 *
 * <p>공유 링크는 새 와이어프레임에서 보류(IA 의 {@code 1k · 링크로 전달})가 됐다.
 * 인증 없이 열리는 경로를 안 쓰면서 열어둘 이유가 없어 통째로 뺐다.
 *
 * <p>남는 것은 전달 화면 하나다. {@code GET /api/cards/{id}/handoff} 는
 * 카드 조회와 응답이 다르다 — {@code evidence} 나 {@code pipelineVersion} 같은
 * 내부 추적값을 의사에게 보여줄 이유가 없다.
 */
package com.jinryomate.backend.handoff;
