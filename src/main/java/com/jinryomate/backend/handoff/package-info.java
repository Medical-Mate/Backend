/**
 * 진료실 전달과 공유. 화면 S4, S6.
 *
 * <p><b>QR 은 MVP 에서 뺐다.</b> 관리자용 화면이 빠지면서 찍을 주체가 사라졌고,
 * 서버가 이미지를 그려주면 병원에서 신호가 약할 때 그 자리에서 표시조차 안 된다.
 *
 * <p>대신 <b>환자가 자기 화면을 의사에게 보여주는</b> 형식이다. S4 는 앱 안에서
 * 끝나고, 백엔드는 그 화면에 쓸 것만 담은 응답을 내려준다 —
 * {@code evidence} 나 {@code pipelineVersion} 같은 내부 추적값은 빼고.
 *
 * <p>공유 링크는 S6(가족 공유·재방문)용으로 남는다. 인증 없이 열리므로
 * <b>짧은 만료가 유일한 방어선</b>이고, 잘못 보냈을 때 끊을 수 있어야 해서
 * 무상태 토큰이 아니라 테이블에 행을 남긴다.
 *
 * <p>공유 응답에 내부 식별자를 담지 않는다. 링크는 어디로든 전달될 수 있다.
 */
package com.jinryomate.backend.handoff;
