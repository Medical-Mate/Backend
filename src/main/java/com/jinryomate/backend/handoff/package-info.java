/**
 * 진료실 전달과 공유. 화면 S4, S6.
 * 
 * <p>관리자용 화면이 MVP에서 빠지면서 QR은 읽기 전용 웹 링크의 다른 표현이 됐다.
 * 토큰 하나에 QR 이미지와 {@code /s/{token}} 주소가 같이 걸린다.
 * 
 * <p>QR 페이로드에는 토큰만 담는다. 이름이나 증상이 들어가면 접수 데스크에서
 * 화면이 찍히는 것만으로 유출된다.
 * 
 * <p>인증 없이 열리는 링크라 <b>짧은 만료가 유일한 방어선</b>이다.
 */
package com.jinryomate.backend.handoff;
