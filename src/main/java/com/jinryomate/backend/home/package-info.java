/**
 * 홈 요약. 화면 1n.
 *
 * <p>자기 데이터를 갖지 않는다. 카드·기록·일정·문답 세션을 <b>모아서 한 번에</b> 내려주는
 * 자리다. 앱이 네 번 호출하면 콜드스타트 때 첫 화면이 네 번 느려진다 — 무료 플랜은
 * 15분 유휴 후 잠들어 첫 요청이 수십 초 걸린다.
 *
 * <p><b>문구를 만들지 않는다.</b> "지난 진료 후 12일이 지났어요"는 앱이 만든다.
 * 서버가 문자열로 내려주면 말투를 바꿀 때마다 배포해야 하고, 시간대가 어긋나면
 * 날짜 수가 하루 틀린다. 숫자와 날짜만 준다. D-day 도 같은 이유로 계산하지 않는다.
 *
 * <p>여러 도메인을 읽으므로 각 도메인의 서비스에 두지 않고 여기로 모은다.
 * {@code card} 가 {@code visit} 을 참조하던 것도 여기로 옮길 후보다.
 */
package com.jinryomate.backend.home;
