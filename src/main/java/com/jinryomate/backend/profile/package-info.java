/**
 * 온보딩과 건강 프로필. 화면 S1.
 * 
 * <p>항목은 이름·나이·성별·복용약·기저질환·알레르기.
 * 
 * <p>주의할 점이 둘 있다.
 * <ul>
 *   <li>"없어요"와 "잘 모르겠어요"는 다른 값이다. null 하나로 뭉개면
 *       의사용 카드에 "알레르기: 본인 확인 못 함"을 찍을 수 없다.</li>
 *   <li>필드마다 출처(self_input / kakao)를 남긴다. 나중에 카카오에서 값을
 *       받아오게 되더라도 사용자가 직접 고친 값을 덮어쓰지 않기 위함이다.</li>
 * </ul>
 */
package com.jinryomate.backend.profile;
