-- 사진 첨부를 지운다.
--
-- 새 와이어프레임에 약봉투(S1)·처방전(S5) 촬영 화면이 없다. #34 에서 공유 링크와
-- 되묻기를 지운 것과 같은 이유다 — 쓰지 않는 업로드 경로를 열어두면 공격 면만 남는다.
--
-- V4 는 지우지 않고 남긴다. 이미 적용된 마이그레이션을 파일에서 없애면
-- Flyway 가 기록과 파일이 어긋난다고 보고 기동을 막는다.
--
-- 되살릴 일이 생기면 bytea 로 돌아가지 않는다. 오브젝트 스토리지에 올리고
-- DB 에는 URL 만 둔다 — 첨부는 회원당 수 MB 라 텍스트(회원당 30KB)와 자릿수가 다르다.
DROP TABLE IF EXISTS attachments;
